import { StyleSheet } from 'react-native';
import { black } from '../../Theme';

const styles = {
  listItem: {
    marginLeft: 0.0,
  },
  badgeText: {
    color: black,
  },
};

export const baseStyles = StyleSheet.create(styles);

export default styles;
